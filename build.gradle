apply plugin: 'com.android.library'

android {
    compileSdkVersion 26
    buildToolsVersion "25.0.3"
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile files('libs/SMART_LOCK_BLUETOOTH_SDK_RELEASE_10.0.1.jar')
}
